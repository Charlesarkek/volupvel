# Python example with multiple mappings
operations = {
    'enqueue': enqueue,
    'dequeue': dequeue,
    'peek': peek,
}

# Call the 'enqueue' function using the dictionary
operations['enqueue'](item)
